server:
  port: 11110

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
zuul:
  routes:
    rabc-service: /rabc/**
  ignored-services: '*'
spring:
  application:
    name: getway
