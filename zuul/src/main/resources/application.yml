server:
  port: 11110

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details:
zuul:
  routes:
    auth-service: /auth/**
    xxl-job-admin: /xxl/**
    rabc-service: /rabc/**
  ignored-services: '*'
spring:
  application:
    name: getway